/*
Challenge 2

Create a function not that takes a 'predicate' function (func) as its only argument
'predicate' means it returns true/false
not should return a function that negates func 
i.e. when func would return true the returned function would return false.

Note that the returned function should be able to handle an arbitrary number of arguments

To test your solution run
npm t not
from the command line

*/

function not(func) {}

module.exports = not;

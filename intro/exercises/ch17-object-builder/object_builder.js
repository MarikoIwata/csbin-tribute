/*
Challenge 17

Create a function objectBuilder that takes in a number and returns 
an object whose keys are 0 up to number and the values are that same 
number multipled by 5.

To test your solution run
npm t object_builder
from the command line

*/

function objectBuilder(count) {}

module.exports = objectBuilder;

/*
Challenge 10

Create a function unique that returns an array where all the duplicates of
the input array have been removed; in other words, every element remaining is unique.

To test your solution run
npm t unique
from the command line

*/

function unique(array) {}

module.exports = unique;

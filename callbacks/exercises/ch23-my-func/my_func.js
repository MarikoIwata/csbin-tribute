/* 
Challenge 23

Define a function myFunc that takes an array and a callback. 
myFunc should pass each element from the array (in order) into the callback. 
If the callback returns true, myFunc should return the index of the 
current element. If the callback never returns true, myFunc should return -1; 

To test your solution run
npm t my_func
from the command line

*/

function myFunc(array, callback) {}

module.exports = myFunc;
